型号是SN里体现
分体机：FC-3200M（支持计时、计次、计流）默认计时（默认参数按格式化后的那些值）


一体机：FC-3500M（支持计时、计次、计流），默认计流。（默认参数按格式化后的那些值）


    一体机自带流量计、分体机不带。


   硬件在安装时选好，到底是计流还是计时然后就不再可以修改。

就是说，生产好，就定义好是什么类型的硬件，再由客户安装时选择好应用模式，之后就不能再去改变。
安装的时候，选择：计次、计流、计时，都要有文字说明，避免客户不知道选错，导致的后面无法正常使用